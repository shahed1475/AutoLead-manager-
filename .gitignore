# Python
__pycache__/
*.py[cod]
*.pyo
venv/
.venv/
*.egg-info/

# Node
node_modules/
dist/
dist-portal/
build/
*.js.map

# Logs & process output
*.log
*.err
*.out
logs/

# Database & data
backend/data/
backend/data/*.db

# Email Campaign uploads (lead files + campaign attachments) — never commit
backend/data/email_campaign_uploads/
email_campaign_uploads/

# Browser automation data
scraper-engine/browser-data/
browser-data/

# Environment secrets (keep .env files tracked for reference but ignore secrets)
# (we keep .env tracked since they only have localhost config, no real secrets)

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude internal (local-only) — but track the project skill system
.claude/*
!.claude/skills/
.superpowers/
.worktrees/

# One-click server runtime state (share link, tunnel pid, logs)
.run/
workspaces/
# Permanent-link tunnel token — secret, never commit
deploy/tunnel.env
