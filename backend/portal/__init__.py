"""Client portal — a separate, client-facing site. Clients sign in with a code
emailed to their Gmail, request leads, and download what the owner delivers.
Served on its own link whose web server forwards only /api/portal/*."""
