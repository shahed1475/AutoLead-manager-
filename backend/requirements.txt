# ── Web framework ────────────────────────────────────────────────────────────
fastapi>=0.115.5
uvicorn[standard]>=0.32.1
python-multipart>=0.0.12

# ── Database ─────────────────────────────────────────────────────────────────
# SQLite is the only DB this app runs on — a single file, ideal for a
# one-click desktop installer. No external DB server to install or manage.
aiosqlite>=0.20.0

# ── AI / LLM ─────────────────────────────────────────────────────────────────
ollama>=0.3.0

# ── Async HTTP + scraping ─────────────────────────────────────────────────────
httpx>=0.27.2
requests>=2.33.1
beautifulsoup4>=4.12.3
lxml>=5.4.0
playwright>=1.48.0
fake-useragent>=1.5.1
python-whois>=0.9.4

# ── Data validation ───────────────────────────────────────────────────────────
pydantic>=2.10.0
pydantic[email]>=2.10.0
pydantic-settings>=2.7.0

# ── Contact verification (Checkpoint 5C) — deterministic, offline/DNS only ───
phonenumbers>=8.13
dnspython>=2.6

# ── Auth & rate limiting ───────────────────────────────────────────────────────
bcrypt>=4.1.0
slowapi>=0.1.9

# ── Testing (dev) ──────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.21.0

# ── At-rest secrets encryption (SMTP/IMAP passwords, cloud LLM API keys) ──────
cryptography>=42.0.0

# ── Email (SMTP send + IMAP receive) ─────────────────────────────────────────
aiosmtplib>=3.0.0

# ── Task scheduling ───────────────────────────────────────────────────────────
apscheduler==3.10.4

# ── WhatsApp automation ───────────────────────────────────────────────────────
pyautogui>=0.9.54
pyperclip>=1.8.2
pywhatkit==5.4
pillow>=11.0.0

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
aiofiles>=24.1.0
urllib3>=2.0.0
selenium>=4.27.1
webdriver-manager>=4.0.2
openpyxl>=3.1
